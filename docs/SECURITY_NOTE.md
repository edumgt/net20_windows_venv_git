# 안전 주의 (수업용)

- 이 프로젝트의 포트 체크/네트워크 코드는 **학습 목적**입니다.
- 허가되지 않은 대상(외부 서버/회사망)에 무분별하게 스캔/부하를 주면 문제가 됩니다.
- 예제는 기본적으로 `example.com` 또는 `127.0.0.1` 같은 안전한 대상 중심으로 구성했습니다.
- 수업에서는 반드시 **allowlist** 개념을 함께 설명하세요.
